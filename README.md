# Assingment project (react + typescript)

## Available Scripts

After clone checkout to `Assignment-project` folder

First run:

### `npm i`

to install all the dependencies.

Then run to 

### `npm run dev`

to start the server.